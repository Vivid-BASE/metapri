# React + Google Apps Script (GAS) 問い合わせフォーム導入ガイド

このガイドでは、React（フロントエンド）とGoogle Apps Script（バックエンド）を使用して、サーバーレスのお問い合わせフォームを実装する方法を解説します。この設定では、送信内容をGoogleスプレッドシートに記録し、Gmailで通知メールを送信します。

## ⚠️ 重要：複数プロジェクトでの使用について
**作業を始める前に（重要！）**
もしこのシステムを別のプロジェクト（例：「MetaPuri」以外）で使用する場合：

1.  **既存のスプレッドシートを再利用しないでください。**
    - 同じシートやスクリプトを使い回すと、異なるプロジェクトの問い合わせが混ざってしまいます。
2.  **プロジェクトごとに新しいスプレッドシートを作成してください。**
3.  **その新しいシートで新しいスクリプトを作成してください。**
4.  **新しいウェブアプリとしてデプロイし、そのプロジェクト専用のURLを取得してください。**

**基本ルール:**
**1プロジェクト = 1スプレッドシート = 1スクリプト = 1つの固有URL**

---

## 1. Google Apps Script (バックエンド)

### セットアップ
1.  新しいGoogleスプレッドシートを作成します。
2.  メニューの `拡張機能` > `Apps Script` をクリックします。
3.  `Code.gs` に以下のコードを貼り付けます（もともと入っているコードは削除してください）。

### コード
**[GAS_CODE.js](GAS_CODE.js) の内容をコピーして、Google Apps Script プロジェクトの `Code.gs` に貼り付けてください。**

このスクリプトは以下の項目を処理するように調整されています：
- `name` (お名前)
- `email` (メールアドレス)
- `message` (メッセージ)
- `honeypot` (スパム対策)
- 自動日付記録

### デプロイの手順（超重要！）
1.  右上の **「デプロイ」** ボタン > **「新しいデプロイ」** をクリックします。
2.  「種類の選択」の歯車アイコンから **「ウェブアプリ」** を選択します。
3.  **次のユーザーとして実行**: `自分` (あなたのGoogleアカウント)。
4.  **アクセスできるユーザー**: `全員` (外部からの送信を受け付けるために必須です)。
5.  **「デプロイ」** をクリックします。
6.  **ウェブアプリのURL** (末尾が `/exec` のもの) をコピーします。

> **注意:** スクリプトを変更した場合は、必ず **「新しいデプロイ」** (デプロイの管理 -> 鉛筆アイコン -> 新しいバージョン) を作成して更新を適用してください。保存するだけでは反映されません。

---

## 2. React/HTML フロントエンド

### 送信ロジック
`index.html` はすでに `fetch` (モード: `no-cors`) を使用するように更新されています。

**必要な作業:**
1.  `index.html` を開きます。
2.  `const GAS_URL = "YOUR_GAS_WEB_APP_URL_HERE";` という行を探します（2988行目付近）。
3.  `"YOUR_GAS_WEB_APP_URL_HERE"` の部分を、先ほどデプロイ手順でコピーした **ウェブアプリのURL** に置き換えてください。

```javascript
// 例
const GAS_URL = "https://script.google.com/macros/s/AKfycbx.../exec"; 
```

## 3. トラブルシューティング

- **CORSエラー**: `no-cors` モードでは、レスポンスの内容やステータスコードを読み取ることはできません（不透明なレスポンスが返ります）。これは正常な動作です。ネットワークエラーが発生しない限り、送信成功とみなします。
- **データが保存されない**:
    - デプロイ時の「アクセスできるユーザー」が **「全員」** になっているか確認してください。
    - `JSON.stringify(data)` で送っているデータ構造と、GAS側の受け取り処理が一致しているか確認してください。
- **メールが届かない**:
    - Gmailの **「送信済み」** フォルダを確認してください。
    - 自分のメールアドレスでテストした場合、Gmailの仕様で受信トレイには表示されず、スレッドにまとめられている場合があります。 **「すべてのメール」** を確認してください。
    - デプロイ時に `MailApp` や `GmailApp` の権限承認を行っているか確認してください。
